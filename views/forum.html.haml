.threads
  %header.forum--header
    .forum--back.mega
      %a{:href=>"/forums/#{params[:id]}"} ←
    .forum--title
      %h2.mega
        = @forum[:title]
  - @threads.each do |thread|
    .thread
      .thread--meta
        %span.ss-icon
          comment
        = thread[:posts_count]
        - if false
          %span.ss-icon
            view
          = thread[:view_count]
      .thread--title
        %a{:href=>thread[:url]}
          = thread[:title]
      - unless thread[:attachments].blank?
        .thread--image
          %img{:src=>thread[:attachments].first[:url]}

%p.original-thread
  %a{:href=>@forum[:original_url]} Original page
  |
  %a{:href=>request.path + ".json"} JSON      